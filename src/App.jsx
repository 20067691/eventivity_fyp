import AppRoutes from './routes/AppRoutes';

function App() {
  return <AppRoutes />;
}

export default App;
// This is the main entry point of the Eventivity application.
// It imports the AppRoutes component, which contains the routing logic for the application.
// The App component renders the AppRoutes, allowing users to navigate between different pages of the application.
